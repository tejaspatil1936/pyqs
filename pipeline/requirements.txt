# Ingestion pipeline dependencies (runs in GitHub Actions, CPU only).
# torch is installed separately in the workflow from the CPU wheel index
# to avoid pulling multi-GB CUDA builds.
requests>=2.31
psycopg2-binary>=2.9
PyMuPDF>=1.24
pytesseract>=0.3.10
Pillow>=10.0
sentence-transformers>=3.0
scikit-learn>=1.4
numpy>=1.26
